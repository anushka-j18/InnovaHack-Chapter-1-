GEMINI_API_KEY=your_gemini_api_key_here

# Recommended for the demo — free tier at https://tavily.com
# Without this, the pipeline falls back to DuckDuckGo, which rate-limits
# more easily since each claim can trigger multiple searches through the loop.
TAVILY_API_KEY=your_tavily_api_key_here
